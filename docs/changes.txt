diff --git a/templates/panos/set_commands/.meta-cnc.yaml b/templates/panos/set_commands/.meta-cnc.yaml
index 8506449..f66b500 100644
--- a/templates/panos/set_commands/.meta-cnc.yaml
+++ b/templates/panos/set_commands/.meta-cnc.yaml
@@ -1,9 +1,9 @@
 # set_commands is used to load a full NGFW configuration
 
 # snippet folder name
-name: skillet_setcommand_panos_v80
+name: skillet_set_command_panos_v81
 # label used for menu selection
-label: v8.0 Iron-Skillet set commands for NGFW
+label: v8.1 Iron-Skillet set commands for NGFW
 description: Iron-Skillet baseline configuration for a firewall
 dependency:
 # type of device configuration
@@ -15,8 +15,6 @@ labels:
   service_type: iron_skillet_set
   collection:
     - IronSkillet
-    - Configure
-    - Best Practice
 
 # variables used in the configuration templates
 # type_hint defines the form field type in pan-cnc
@@ -80,7 +78,7 @@ variables:
     type_hint: ip_address
   - name: INTERNET_ZONE
     description: untrust zone to filter out in reports
-    default: internet
+    default: untrust
     type_hint: text
   - name: EMAIL_PROFILE_GATEWAY
     description: email gateway address for critical alerts
diff --git a/templates/panos/set_commands/iron_skillet_panos_full.conf b/templates/panos/set_commands/iron_skillet_panos_full.conf
index b93351d..ce2f957 100644
--- a/templates/panos/set_commands/iron_skillet_panos_full.conf
+++ b/templates/panos/set_commands/iron_skillet_panos_full.conf
@@ -1,5 +1,5 @@
-# set command configuration for panos version 8.0
-# commands are expected to be load in order
+# set command configuration for panos version 8.1
+# commands are expected to be loaded in order
 # this template uses jinja format
 # either 'search and replace' or the scripts in the tools dir can be used to create a loadable configuration
 
@@ -60,7 +60,7 @@ set deviceconfig system type dhcp-client accept-dhcp-hostname no
 set deviceconfig system type dhcp-client accept-dhcp-domain no
 set deviceconfig setting ctd strip-x-fwd-for yes
 set deviceconfig setting ctd x-forwarded-for yes
-set deviceconfig setting ctd skip-block-http-range yes
+set deviceconfig setting ctd allow-http-range no
 set deviceconfig setting ctd tcp-bypass-exceed-queue no
 set deviceconfig setting ctd udp-bypass-exceed-queue no
 set deviceconfig setting management enable-log-high-dp-load yes
@@ -78,6 +78,7 @@ set deviceconfig setting wildfire file-size-limit flash size-limit 5
 set deviceconfig setting wildfire file-size-limit MacOSX size-limit 1
 set deviceconfig setting wildfire file-size-limit archive size-limit 10
 set deviceconfig setting wildfire file-size-limit linux size-limit 2
+set deviceconfig setting wildfire file-size-limit script size-limit 2000
 set deviceconfig setting wildfire report-benign-file yes
 set deviceconfig setting wildfire report-grayware-file yes
 set deviceconfig setting config rematch yes
diff --git a/templates/panos/set_commands/iron_skillet_panos_full.xlsx b/templates/panos/set_commands/iron_skillet_panos_full.xlsx
index a22998a..d7faff1 100644
Binary files a/templates/panos/set_commands/iron_skillet_panos_full.xlsx and b/templates/panos/set_commands/iron_skillet_panos_full.xlsx differ
